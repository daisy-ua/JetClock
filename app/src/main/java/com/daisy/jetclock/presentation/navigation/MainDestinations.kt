package com.daisy.jetclock.presentation.navigation

enum class MainDestinations {
    ALARMS_ROUTE,
    ALARM_ROUTE,
    SELECT_SOUND_ROUTE,

    ALARM_ID_KEY,
    SOUND_ID_KEY,

    ALARM_DETAILS_GRAPH,
}
